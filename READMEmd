### pip install 
```
pip install python-venv
python -m venv venv (가상환경 설치)
./venv/Scripts/activate (가상환경 실행 Windows 환경)
./venv/bin/activate (가상환경 실행 Linux 환경)
deactivate (가상환경 종료)


pip install -r requirements.txt (라이브러리 설치)
python main.py (실행)

```


